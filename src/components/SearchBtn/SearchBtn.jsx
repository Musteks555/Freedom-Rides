import css from "./SearchBtn.module.css";

const SearchBtn = () => {
    return (
        <button type="button" className={css.btn}>
            Search
        </button>
    );
};

export default SearchBtn;
